All ranks are perfectly balanced — identical byte deltas across all 4 ranks (ratio 1.00) and uniform SM clocks (ratios 0.99–1.01). No throughput drop, no rank stall, no clock clamp.

**Diagnosis: no fault detected — cluster is healthy.**

Evidence:
- Telemetry pipeline is up (otel-collector, Prometheus, and all node/GPU exporters healthy); only `vllm-*` targets are down, which is expected/benign.
- `nccl_profiler_collective_bytes_total` is advancing steadily across all 4 ranks over the last 2 minutes, with no flatlining.
- Per-rank comparison shows all ranks delta-matched (ratio 1.00) — no dead or stalled rank.
- GPU SM clocks are uniform across ranks (~2790–2820 MHz, ratio 0.99–1.01) — no clock clamp on any GPU.

Ruled out: collector death, dead rank, and clock-clamped GPU. Nothing in the current window points to an active fault on hosts `bravo` or `golf`.
