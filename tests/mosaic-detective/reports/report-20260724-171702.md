All four ranks show identical collective byte deltas (ratio 1.00) and uniform SM clocks across both hosts (bravo, golf), with the telemetry pipeline fully healthy.

**Diagnosis: no active fault.** The cluster is operating normally.

- Telemetry: `otel-collector` and `prometheus` targets are UP; the two DOWN targets (`vllm-golf`, `vllm-bravo`) are the expected idle state, not a fault.
- Throughput: `nccl_profiler_collective_bytes_total` is climbing steadily on all 4 ranks (~9.07GB delta each over 2 min), no rank has stalled.
- GPU health: SM clocks are uniform across ranks (~2790–2816 MHz, ratio ~1.00) — no clock clamp on either host.

Ruled out: dead rank/collector (counters are all incrementing), collector outage (both up), and GPU clock throttling (no ratio deviation). If you were expecting to see a specific incident, let me know the approximate time and I can widen the window and re-check.
