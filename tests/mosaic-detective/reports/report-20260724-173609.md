**Diagnosis: no active fault — the cluster is healthy.**

Evidence:
- **Telemetry pipeline**: `otel-collector` and `prometheus` are UP; only `vllm-golf`/`vllm-bravo` are DOWN, which is the expected idle state for those targets — ruled out as a cause.
- **Overall throughput**: `nccl_profiler_collective_bytes_total` is incrementing steadily across all 4 ranks over the last 2 minutes — no stall.
- **Per-rank balance**: all 4 ranks show identical byte deltas (ratio 1.00) — no dead or lagging rank.
- **Clock clamp check**: `DCGM_FI_DEV_SM_CLOCK` means are uniform across all 4 GPUs (ratios 0.99–1.01, all ~2790–2820 MHz) — no GPU is clock-limited.
- **15-minute anomaly scan**: the "anomalies" found are not faults — the NCCL byte-rate alternates between two values on a regular ~15s cadence for the entire 15-minute window (the normal rhythm of collective batches, not a step change), and the one DCGM clock wobble (bravo rank 1, 2812↔2820 MHz) is sub-1% jitter, consistent with normal clock boost behavior, not a sustained clamp.

Nothing points to a dead rank, network degradation, GPU clamp, or telemetry gap. The cluster is operating normally at the current time.
