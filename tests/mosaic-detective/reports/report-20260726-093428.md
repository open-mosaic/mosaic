**Diagnosis: no active fault — the cluster is healthy.**

Evidence:
- **Telemetry pipeline alive:** `otel-collector` and `prometheus` are UP. Only `vllm-golf`/`vllm-bravo` are DOWN, which is normal idle-inference noise, not diagnostic.
- **Throughput normal:** `nccl_profiler_collective_bytes_total` is advancing on all 4 ranks (rank 0–3 across bravo/golf) over the last 2 minutes, with delta ratio 1.00 across every rank — no stalled or frozen counter.
- **No clock clamp:** `DCGM_FI_DEV_SM_CLOCK` mean is ~2790–2820 MHz on all 4 ranks (ratios 0.99–1.01) — no GPU is throttled relative to its peers.
- **No network degradation:** since clocks are uniform and healthy *and* throughput is at full rate (not a uniform step-down from baseline), the netem-style degradation signature doesn't apply either.

Ruled out: dead rank/job stop (counters are actively growing, not frozen), collector kill (target UP, counters advancing), GPU clock clamp (all ranks uniform), network degradation (throughput at full rate, not reduced).

One note: a 15-minute `--find-anomaly` scan showed a recurring rate oscillation (~78.9 MB/s ↔ 52.6 MB/s) at ~09:23, 09:28, and 09:33, but it's identical in magnitude and timing across all 4 ranks — consistent with a normal periodic duty cycle (e.g. compute/comm phase alternation) rather than a fault, and it isn't present in the current window. No corrective action needed.
