All 4 GPUs' SM clocks are uniform (ratio ~1.0 across ranks) — no clock clamp. Combined with normal, matched throughput deltas across all ranks, this cluster looks healthy right now.

**Diagnosis: No active fault.** 

Evidence:
- Telemetry pipeline is up (otel-collector healthy; only idle `vllm-*` targets are down, which is expected).
- `nccl_profiler_collective_bytes_total` is increasing steadily and uniformly across all 4 ranks (bravo:0/1, golf:2/3) over the last 2 minutes — no stalled rank.
- `DCGM_FI_DEV_SM_CLOCK` is uniform across all ranks (ratio 0.99–1.01) — no clock clamp on any GPU.

Ruled out: dead collector, stalled rank/process, and GPU clock throttling. The cluster is operating normally at this time.
