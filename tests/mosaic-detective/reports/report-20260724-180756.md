All ranks show uniform SM clocks (ratio ~1.00) and identical collective bytes deltas across all 4 ranks. No stalled rank, no clock clamp, no telemetry gap. This cluster looks healthy right now.

**Diagnosis: No active fault.**

- Telemetry pipeline: `otel-collector` UP; only `vllm-golf`/`vllm-bravo` are DOWN, which is the normal idle state — ruled out as a cause.
- Throughput: `nccl_profiler_collective_bytes_total` is advancing steadily and identically across all 4 ranks (delta ≈ 9.07e9 bytes each, ratio 1.00) — no stall, no rank dropped out.
- Clock health: `DCGM_FI_DEV_SM_CLOCK` means are uniform across ranks (2790–2820 MHz, ratio 0.99–1.01) — no GPU is clock-clamped.

Nothing in the last 2 minutes points to a rank death, network degradation, or telemetry gap. Everything checked is nominal — no fault to report.
