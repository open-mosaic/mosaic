**Diagnosis: No fault — the cluster is healthy.**

Evidence:
- **Telemetry pipeline**: All collectors and exporters are UP (`otel-collector`, `prometheus`, both GPU/node/process exporters on bravo and golf). Only `vllm-golf`/`vllm-bravo` are DOWN, which is expected/normal and unrelated to cluster health.
- **Throughput**: `nccl_profiler_collective_bytes_total` is advancing steadily on all 4 ranks, with byte deltas over the last 2 minutes identical across ranks (ratio 1.00 each) — no stalled or lagging rank.
- **GPU clocks**: `DCGM_FI_DEV_SM_CLOCK` is uniform across all 4 ranks (ratios 0.99–1.01) — no clock clamp on any GPU.
- **15-minute scan**: The `--find-anomaly` sweep flagged rate shifts, but they're a synchronized, periodic oscillation (78.8M/s ↔ 52.6M/s) recurring every ~5 minutes on *all four ranks simultaneously* with matching timestamps — consistent with normal batch/step boundaries in the training workload, not a fault (a real fault would show up as one rank or GPU diverging from its peers, which it doesn't here).

Ruled out: dead collector, stalled rank, GPU clock clamp, and network/link degradation (all-reduce is synchronized and uniform across both hosts, bravo and golf). Nothing in the current or recent window indicates an active or recently-resolved fault.
