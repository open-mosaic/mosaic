**Diagnosis: no active fault — cluster is healthy.**

Evidence:
- **Collector health**: `otel-collector` and `prometheus` UP; NCCL counters are actively advancing (confirmed via delta across a 2-min window). Only `vllm-*` targets are down, which is expected/ignorable.
- **Throughput**: all 4 ranks (bravo rank 0/1, golf rank 2/3) show identical collective-bytes deltas over the last 2 minutes (ratio 1.00 across the board) — no stalled or dead rank.
- **GPU clocks**: `DCGM_FI_DEV_SM_CLOCK` is uniform across all 4 GPUs (2790–2820 MHz, ratio 0.99–1.01) — no clock clamp on any rank.
- **15-minute history**: I did find a recurring rate oscillation (~78.9 MB/s ↔ ~52.6 MB/s, flipping roughly every 5 minutes, in sync across all ranks), but it doesn't match any known fault signature — a clock clamp is a persistent step-change on one GPU (not seen here, clocks never moved), and network degradation (netem) is a persistent uniform step-down (not a repeating oscillation that keeps recovering). SM clocks were flat the entire 15 minutes with zero rate changes detected. The most recent 2-minute window sits at the healthy rate (~75.6 MB/s).

Ruled out: telemetry gap (collector alive, counters moving), rank/job death (no freeze), GPU clock clamp (clocks uniform throughout), and network degradation (no persistent throughput step-down — current rate is back to baseline).

The periodic wobble looks like normal training-loop cadence (e.g., a periodic barrier or lighter compute phase) rather than an infrastructure fault, but flagging it since it's the only irregularity in an otherwise clean signal.
