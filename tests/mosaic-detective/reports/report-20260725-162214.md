**Diagnosis: transient full communication stall, already resolved — not a current fault.**

**What I checked:**
- **Telemetry pipeline:** `otel-collector` and `prometheus` are both UP; `vllm-*` targets down is expected/ignorable. NCCL counters are actively advancing, confirming live data (not stale).
- **Current state (last 2–3 min):** All 4 ranks (bravo:0,1 / golf:2,3) show identical throughput deltas (ratio 1.00) and identical SM clocks (ratio ~1.00–1.01) — no dead rank, no clock clamp, cluster is healthy right now.
- **15-min history:** `nccl_profiler_collective_bytes_total` shows a normal bursty oscillation (~79MB/s ↔ ~53MB/s) throughout, plus one brief anomaly: at **16:20:26** the rate on all 4 ranks dropped simultaneously to **0 B/s**, then recovered by **16:20:41–16:21:11**, ramping back up through the normal oscillation band.

**Interpretation:** The stall hit all ranks identically at the same timestamps — consistent with a single synchronized collective operation (all-reduce) briefly hanging, most likely a transient interconnect hiccup rather than a GPU-side clock clamp (clocks stayed uniform before/after) or a dead rank (no lasting counter freeze, no single outlier). It self-recovered within ~15 seconds and throughput has been normal since.

**Ruled out:** dead collector, dead rank, GPU clock clamp, ongoing degradation — all current signals are uniform and healthy. This was a brief, already-resolved hiccup around 16:20:26–16:20:41.
