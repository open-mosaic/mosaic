**Diagnosis: No active fault — the cluster is healthy.**

Evidence:
- **Telemetry pipeline:** `otel-collector` UP; only `vllm-golf`/`vllm-bravo` are DOWN, which is normal idle state (no inference workload running).
- **Throughput:** All 4 ranks' collective-bytes counters are advancing steadily, byte-identical across ranks over the last 2 minutes (delta ratio 1.00 across bravo rank0/1 and golf rank2/3).
- **GPU clocks:** `DCGM_FI_DEV_SM_CLOCK` is uniform across all 4 GPUs (2790–2820 MHz, ratio 0.99–1.01) — no clock clamp.
- **15-minute anomaly scan:** shows a recurring rate oscillation (78.9 MB/s ↔ 52.6 MB/s) that is perfectly synchronized across all 4 ranks and repeats on a regular ~5-minute cadence (17:53, 17:58, 18:03). This doesn't match any fault signature — it isn't a step-down to a fixed degraded value (rules out network degradation/netem), it isn't a freeze (rules out collector kill / rank kill), and it isn't isolated to one rank/node (rules out clock clamp). It reads as normal periodic batch/step cadence in the training loop.

Ruled out: telemetry gap, dead rank, clock clamp, network degradation — none of their signatures are present. Nothing needs attention right now.
