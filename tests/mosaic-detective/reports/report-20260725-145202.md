All 4 ranks show identical deltas (ratio 1.00) — perfectly balanced, no rank lagging.

**Diagnosis: no active fault.** The cluster looks healthy right now.

- **Telemetry**: `otel-collector` and `prometheus` are up; only `vllm-golf`/`vllm-bravo` are down, which is expected/benign and unrelated to collective health.
- **Collective throughput**: all 4 ranks (bravo:0,1; golf:2,3) are advancing `nccl_profiler_collective_bytes_total` at identical rates over the last 2 minutes (delta ratio 1.00 across the board) — no stalled or dead rank.
- **GPU clocks**: `DCGM_FI_DEV_SM_CLOCK` is uniform across ranks (~2790–2818 MHz, ratio ~0.99–1.00) — no clock clamp on any GPU, ruling out a hidden per-GPU slowdown masked by all-reduce synchronization.

Ruled out: collector death, dead/stalled rank, and GPU clock throttling. No further widening of the window needed since the evidence is unambiguous — the cluster is operating normally.
