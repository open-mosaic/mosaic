**Diagnosis: no active fault — the cluster is currently idle after a job completed normally roughly 24 hours ago.**

Evidence:
- **Collector health:** `otel-collector`, `prometheus`, and both `gpu_exporter`/`node_exporter`/`process_exporter` targets are UP. Only `vllm-golf`/`vllm-bravo` are DOWN, which is expected when no inference workload is running.
- **NCCL metrics (last 2m–4h):** completely absent. Widening to 24h showed data *did* exist earlier, ruling out a labeling/query error.
- **Taper pattern (all 4 ranks, both communicators):** collective-bytes rate held a steady oscillating pace (~53–79 MB/s) for hours, then between **2026-07-24 14:42 and 14:55** it stepped down through several decreasing values (78M → 52M → 26M → 0, with a couple of brief trailing spikes) before flatlining at 0.00/s — a multi-minute taper, not an instantaneous drop.
- **GPU clocks (DCGM):** all 4 GPUs (golf and bravo, both devices) ran a healthy ~2790–2835 MHz throughout the job, then idled down to 180 MHz in the same window (golf: 14:38–14:39 and 14:50–14:51; bravo: 12:35 and 14:50–14:51) — both nodes settled to idle together, not staggered by the 10–20s one-rank-first pattern that would indicate a killed rank.

Ruled out:
- **Collector kill** — `otel-collector` is UP now and there's no indication it was down during the transition.
- **Rank kill** — no single rank stopped measurably earlier than its peers; all four idled down within the same multi-minute window, and the throughput tapered rather than dropping abruptly from steady state.
- **Clock clamp / network degradation** — not applicable; these describe a *running* job with reduced throughput, but this job has fully wound down and no collectives are occurring at all.

Bottom line: the training job finished around **2026-07-24 14:50** (about 24h before now), telemetry was healthy throughout, and the cluster has simply been idle since — the absent NCCL data is the expected look of "no workload," not a fault.
