All four ranks show the rate dropping to 0.00/s at ~11:18:33–11:18:48, after normal periodic rate oscillation (a routine artifact of the collective pattern, not a fault) throughout the rest of the window. This pinpoints when the collector died.

Diagnosis: the telemetry pipeline died — the `otel-collector` process on `localhost:8889` stopped around **2026-07-26 11:18:33–11:18:48**, not the GPU workload itself.

Evidence:
- `check_collector_health.py`: `otel-collector` scrape target is DOWN.
- The NCCL collective-bytes counters looked "recent" at first glance, but two back-to-back queries seconds apart returned byte-identical `last` values for all 4 ranks — the counters are frozen, not advancing.
- `--find-anomaly` over 15 minutes shows all 4 ranks' rate dropping cleanly to 0.00/s at 11:18:33–11:18:48, right after otherwise-normal periodic rate oscillation (78.8M/s ↔ 52.6M/s, a benign artifact of the collective's pattern).

Ruled out: `vllm-golf`/`vllm-bravo` being DOWN is expected/unrelated. This isn't a GPU clock clamp or a single dead rank — all 4 ranks froze at the exact same moment, which points to the shared collector process, not the workload. I can't confirm from these metrics alone whether the collector crashed or was killed — that would require host/process-level logs outside what these scripts expose.
